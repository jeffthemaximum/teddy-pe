{
  "name": "teddy-pe",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Teddy's Training Year. A Rails API in backend/, the shared Redux package in core/, the React web app in web/, and the project's memory in docs/.",
  "engines": { "node": ">=20" }
}
